# update_os - Update system packages (Debian/Ubuntu)
# Usage: update_os
# Must be run as root or with sudo

if (( EUID != 0 )); then
  sudo "$ZFUNC/update_os"
  return $?
fi

apt-get update
apt-get -y upgrade
apt-get -y dist-upgrade
apt-get clean
apt-get -y autoremove
