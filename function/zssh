# zssh - Display zsh installation info
# Usage: zssh

local ZSH_DIR=$(whereis zsh | awk '{print $2}')

if [[ -z "$ZSH_DIR" ]]; then
    echo "Zsh is not installed."
    return 1
fi

echo "Zsh is installed at: $ZSH_DIR"
$ZSH_DIR --version
